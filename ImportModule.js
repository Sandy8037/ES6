// ES6 Import/Export
export function sum(x, y) {
  return x + y;
}
export var pi = 3.141593;

// ES5 Import/Export
// var LibMath = {};
// LibMath.sum = function (x, y) {
//   return x + y;
// };
// LibMath.pi = 3.141593;
